// ? Sorting Algorithm snippets for Programming languages...
{
	"BUBBLE_SORT(C++)": { //! BUBBLE SORT snippet(C++)...
		"scope": "cpp",      //* A list of language names to which this snippet applies, e.g. 'typescript,javascript,c,c++'.
		"prefix": "sort",    //* The prefix to use when selecting the snippet in intellisense...
		"body": [            //* Code that will be generated...
			"int* bubbleSort(int* A, int n)",
			"{",
			"\tfor(size_t i{1}; i<n; i++) {",
			"\t\tfor(size_t j{0}; j<n-i; j++) {",
			"\t\t\tif(A[j] > A[j+1])",
			"\t\t\t{",
			"\t\t\t\tint temp = A[j];",
			"\t\t\t\tA[j] = A[j+1];",
			"\t\t\t\tA[j+1] = temp;",
			"\t\t\t}",
			"\t\t}",
			"\t}",
			"\treturn A;",
			"}",
			"$2"
		],
		"description": "This is a bubble sort function template, for C++ Programming..." //* The snippet description.
	},
	"BUBBLE_SORT(C)": { //! BUBBLE SORT snippet(C)...
		"scope": "c",
		"prefix": "sort",
		"body": [
			"int* bubbleSort(int* A, int n)",
			"{",
			"\tint i, j, temp;",
			"\tfor(i=1; i<n; i++) {",
			"\t\tfor(j=0; j<n-i; j++) {",
			"\t\t\tif(A[j] > A[j+1])",
			"\t\t\t{",
			"\t\t\t\ttemp = A[j];",
			"\t\t\t\tA[j] = A[j+1];",
			"\t\t\t\tA[j+1] = temp;",
			"\t\t\t}",
			"\t\t}",
			"\t}",
			"\treturn A;",
			"}",
			"$2"
		],
		"description": "This is a bubble sort function template, for C Programming..."
	},
	"SELECTION_SORT(C++)": { //! SELECTION SORT snippet(C++)...
		"scope": "cpp",
		"prefix": "sort",
		"body": [
			"int* selectionSort(int* A, int n)",
			"{",
			"\tfor(size_t i{0}; i<n-1; i++) {",
			"\t\tfor(size_t j{i+1}; j<n; j++) {",
			"\t\t\tif(A[j] < A[i])",
			"\t\t\t{",
			"\t\t\t\tint temp = A[j];",
			"\t\t\t\tA[j] = A[i];",
			"\t\t\t\tA[i] = temp;",
			"\t\t\t}",
			"\t\t}",
			"\t}",
			"\treturn A;",
			"}",
			"$2"
		],
		"description": "This is a selection sort function template, for C++ Programming..."
	},
	"SELECTION_SORT(C)": { //! SELECTION SORT snippet(C)...
		"scope": "c",
		"prefix": "sort",
		"body": [
			"int* selectionSort(int* A, int n)",
			"{",
			"\tint i, j, temp;",
			"\tfor(i=0; i<n-1; i++) {",
			"\t\tfor(j=i+1; j<n; j++) {",
			"\t\t\tif(A[j] < A[i])",
			"\t\t\t{",
			"\t\t\t\ttemp = A[j];",
			"\t\t\t\tA[j] = A[i];",
			"\t\t\t\tA[i] = temp;",
			"\t\t\t}",
			"\t\t}",
			"\t}",
			"\treturn A;",
			"}",
			"$2"
		],
		"description": "This is a selection sort function template, for C Programming..."
	},
	"INSERTION_SORT(C++)": { //! SELECTION SORT snippet(C)...
		"scope": "cpp",
		"prefix": "sort",
		"body": [
			"int* insertionSort(int* A, int n)",
			"{",
			"\tfor(int i=1; i<n; i++)",
			"\t{",
			"\t\tint current = A[i];",
			"\t\tint j = i-1;",
			"\t\twhile((A[j] > current) && (j>=0))",
			"\t\t{",
			"\t\t\tA[j+1] = A[j];",
			"\t\t\tj--;",
			"\t\t}",
			"\t\tA[j+1] = current;",
			"\t}",
			"\treturn A;",
			"}",
			"$2"
		],
		"description": "This is a insertion sort function template, for C++ Programming..."
	},
	"INSERTION_SORT(C)": { //! SELECTION SORT snippet(C)...
		"scope": "c",
		"prefix": "sort",
		"body": [
			"int* insertionSort(int* A, int n)",
			"{",
			"\tint i, j, current;",
			"\tfor(i=1; i<n; i++)",
			"\t{",
			"\t\tcurrent = A[i];",
			"\t\tj = i-1;",
			"\t\twhile((A[j] > current) && (j>=0))",
			"\t\t{",
			"\t\t\tA[j+1] = A[j];",
			"\t\t\tj--;",
			"\t\t}",
			"\t\tA[j+1] = current;",
			"\t}",
			"\treturn A;",
			"}",
			"$2"
		],
		"description": "This is a insertion sort function template, for C Programming..."
	}
}